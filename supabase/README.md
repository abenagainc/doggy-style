# Supabase

This directory contains the version-controlled Supabase implementation.

The database schema should be created through migrations derived from `docs/technical/20-*`, not by manually creating production tables in the dashboard.

Do not add credentials or secrets to this directory.
